config = dict()

config["HOST"] = "localhost"

# URLS Returned
config["DOMAIN"] = "localhost"
config["PORT"] = 5000

# Disable for production use
config["DEBUG"] = False

# User Auth
config["KEY"] = ""


